---
name: Publish Docker Studio
'on':
  push:
    tags:
      - '*'
jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
        with:
          ref: 3.4.0
      - uses: ./.github/actions/publish-docker-studio-action
        env:
          DOCKERHUB_TOKEN: ${{ secrets.STUDIO_DOCKERHUB_TOKEN }}
